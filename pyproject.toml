[project]
name = "philex"
version = "0.1.0"
description = "Broadway Lounge site on Cloudflare Python Workers (FastAPI)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-multipart",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
